#include <iostream>

int main() {
  char name = "";
  int count = 0;
  
  std::cout << "What is your name?" << std:endl;
  std::cin >> 

  std::cout << "How many tigers should I display, " <<   << "?" << std:endl;
  std::cout << "Ok, " << name << ", here are your " << count << "tigers:" << std::endl;

  for (int i = 0; i < count; i++) {
    
  }
}
